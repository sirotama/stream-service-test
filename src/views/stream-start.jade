extends ./base
block head
    script.
        $(function(){
            $("#rtmpurl").val("rtmp://"+location.hostname+"/live/")
            $("#streamKeyShow").click(function(){
                prompt("please copy",$("#streamKey").val())
            })
        })
block content
    h1 配信開始
    form(method="POST")
        .form-group
            label 配信タイトル
            input(type="text",name="name",value=user.newStream.name).form-control
        .form-group
            label 説明
            textarea(name="description").form-control= user.newStream.description
        button(type="submit").btn.btn-primary 配信情報を更新
    p ↓使って、どうぞ
    .form-group
        label RTMP URL
        input(type="text",value="rtmp://fuck.local/live/").form-control#rtmpurl
    .form-group
        label RTMP Stream Key(ないしょのやつ)
        .input-group
            input(type="password",value=user.streamKey).form-control#streamKey
            span.input-group-btn
                button.btn.btn-secondary#streamKeyShow 表示
    h2 推奨設定
    ul
        li ビットレート:1500kbps前後（サーバーサイドで1250kbpsに再エンコされます）
